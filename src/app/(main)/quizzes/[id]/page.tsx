// Cái này chưa chắc để xem lại sau

export default async function QuizDetailPage({
  params,
}: {
  params: Promise<{ id: string }>
}) {
  return (
    <div>
      <h1>Quiz Detail - TBD</h1>
    </div>
  )
}
